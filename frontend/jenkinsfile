pipeline {
    agent any

    environment {
        DOCKER_IMAGE = 'yourdockerhubusername/react-frontend'
    }

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/yourusername/your-react-repo.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    dockerImage = docker.build("${DOCKER_IMAGE}:latest")
                }
            }
        }

        stage('Push Docker Image') {
            steps {
                script {
                    docker.withRegistry('', 'dockerhub-credentials-id') {
                        dockerImage.push()
                    }
                }
            }
        }

        stage('Run Container') {
            steps {
                sh '''
                docker stop react-frontend || true
                docker rm react-frontend || true
                docker run -d --name react-frontend -p 80:80 ${DOCKER_IMAGE}:latest
                '''
            }
        }
    }
}
